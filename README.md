# Dependency Injection with Java
This repo I want to build from scratch a simple dependency injection framework. Using with `singleton`, `factory` design patterns.

## Structure of a Dependency in Java
